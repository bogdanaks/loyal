import { StyleSheet, Text } from "react-native"
import { ScreenContainer } from "widgets/ui/screen-container"

export const UserAgreementScreen = () => {
  return (
    <ScreenContainer>
      <Text>UserAgreementScreen</Text>
    </ScreenContainer>
  )
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
  },
})
